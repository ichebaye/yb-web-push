{
  "name": "yb-web-push",
  "private": true,
  "version": "0.1.0",
  "description": "Minimal static test rig: installable iOS PWA that tries to hand a notification tap off to Yandex Browser via its deep-link URL scheme.",
  "scripts": {
    "generate-icons": "node scripts/generate-icons.js",
    "serve": "npx --yes http-server public -p 3000"
  }
}
